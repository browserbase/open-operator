# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Browserbase Configuration
BROWSERBASE_API_KEY=your_browserbase_api_key_here
BROWSERBASE_PROJECT_ID=your_browserbase_project_id_here

# Google Maps API Configuration
# To enable address autocomplete, add your Google Maps API key here:
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Instructions for Google Maps setup:
# 1. Go to https://console.developers.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the "Places API" and "Maps JavaScript API"
# 4. Create an API key and restrict it to your domain
# 5. Add the key above and restart your development server

# Firebase Admin SDK Configuration
# To enable mileage data storage, add your Firebase service account key:
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id"...}

# Instructions for Firebase setup:
# 1. Go to https://console.firebase.google.com/
# 2. Go to Project Settings > Service Accounts
# 3. Click "Generate new private key" 
# 4. Copy the entire JSON content and paste it as the value above (as a single line)
# 5. Make sure your Firestore database allows writes from the Admin SDK
